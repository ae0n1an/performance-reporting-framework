# Copy to .env and adjust values

# Postgres
POSTGRES_USER=loadrunner
POSTGRES_PASSWORD=secret
POSTGRES_DB=loadrunner

# App
SECRET_KEY=change-me-in-production
APP_PORT=8000

# Dev only — exposes Postgres to host machine
DB_PORT=5432

# Connection pool
DB_POOL_MIN=2
DB_POOL_MAX=10
